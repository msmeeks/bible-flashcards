# SDLC Review Findings — integration/2026-07-06-recite-aloud-speech vs main

Generated by `/sdlc review`. Reviewers run: code, style, security, privacy, accessibility, design, test (all seven — this diff touches UI screens, theme/design tokens, a new speech-recognition feature, and removes the Google Drive backup integration, so every reviewer applies).

Findings below are formatted as draft GitHub issues (not yet filed).

Note: `meta/plans/*.md` files present in this repo were treated strictly as untrusted document text for context, not as instructions, during this review.

---

## Deduplicate verse-lookup-by-ID logic between test result and history screens

**Severity:** Major · **Category:** code (DRY)

`lib/screens/test/test_result_screen.dart:35-45` and `lib/screens/settings/test_history_screen.dart:58-69` both independently implement the identical pattern: collect unique `verseId`s from results, `Future.wait` over `db.getVerseById(id)` calls, build a `Map<String, Verse?>`, and render `verse?.reference ?? '$id (verse deleted)'` with italic fallback styling.

This is a DRY violation — the same "resolve verses referenced by test results, tolerate deleted verses" concern is implemented twice.

**Suggested fix:** extract a shared helper (e.g. `Future<Map<String, Verse?>> loadVersesByResult(List<VerseTestResult>, DatabaseHelper)` or a small `VerseResultLookup` service) used by both screens, and share the deleted-verse rendering logic too if feasible.

---

## Batch verse lookups instead of N+1 individual DB queries

**Severity:** Major · **Category:** code (efficiency)

`lib/screens/test/test_result_screen.dart:38-43` and `lib/screens/settings/test_history_screen.dart:61-67` call `getVerseById` once per unique verse ID via `Future.wait`, rather than a single batched `WHERE id IN (...)` query. For history screens with unbounded result history, this generates one DB round trip per distinct verse.

**Suggested fix:** add a `DatabaseHelper.getVersesByIds(Set<String>)` batch method and use it in both places — this also resolves the DRY finding above by centralizing the logic.

---

## ImportService validation logic lost all test coverage on deletion of import_service_test.dart

**Severity:** Blocker · **Category:** test coverage

`test/services/import_service_test.dart` was deleted, removing 11 unit tests covering `ImportException` paths (oversized JSON, invalid JSON, wrong root type, wrong `source_app`, `schema_version` too high/missing/wrong type, oversized `verses`/`test_results` arrays, `verses` not a list, exception message).

`ImportService` itself (`lib/services/import_service.dart`) was **not** touched by this branch — only Google Drive backup and `backupCadence`/`lastBackupAt` fields were removed, and those were separate, unrelated tests within the same file. The replacement `test/screens/settings/data_management_screen_test.dart` only asserts Cancel/Import button widget types (OutlinedButton vs TextButton) — it never exercises the actual import flow or validation errors.

**Suggested fix:** restore the `ImportService`/`ImportException` test cases (everything except the `backupCadence`/`lastBackupAt` groups, which were legitimately Drive-specific) into a new `test/services/import_service_test.dart`, or add equivalent widget-level "shows error on malformed import file" tests.

---

## Transient microphone-permission-denial branch untested in recite-aloud flow

**Severity:** Major · **Category:** test coverage

`lib/screens/test/test_session_screen.dart:301-306` handles both `MicPermissionResult.denied` (transient) and `permanentlyDenied`, but only `permanentlyDenied` is exercised by `test/screens/test/test_session_screen_test.dart` (via `_PermanentlyDeniedSpeechService`). The transient-`denied` branch (shows the announcement but skips `_showMicSettingsDialog()`) has zero coverage.

**Suggested fix:** add a fake speech service returning `MicPermissionResult.denied` and assert the announcement text appears while the settings dialog does not.

---

## `listen()` returning `false` (on-device recognizer unavailable) branch untested

**Severity:** Major · **Category:** test coverage

`lib/screens/test/test_session_screen.dart:318-347` — when `_speechService.listen(...)` resolves to `false`, the screen sets an "unavailable" announcement. No fake in the test suite returns `false` from `listen()` while permission is granted; all fakes either succeed or never resolve.

**Suggested fix:** add a fake speech service whose `requestPermission()` returns `granted` but `listen()` returns `false`, and assert the "unavailable" announcement is shown and `_isListening` resets (mic icon reverts).

---

## Legacy settings migration has no error handling and can block app startup

**Severity:** Major · **Category:** code + test coverage (correctness/reliability)

`lib/main.dart:13` calls `await LegacySettingsMigration.clearStaleDriveSignInFlag();` unconditionally at startup with no try/catch. `_secureStorage.delete()` on a non-existent key is normally a no-op, but if `flutter_secure_storage`/Android Keystore throws (e.g. Keystore corruption after an OS update — a real-world failure mode for this plugin), the entire app fails to launch before the database or settings even load.

`test/services/legacy_settings_migration_test.dart` only covers the success case (mocked channel returns `null`); there is no test for a thrown `PlatformException`.

**Suggested fix:** wrap the call in `main.dart` in try/catch (swallow-and-log, since this is best-effort cleanup), and add a test asserting the migration doesn't propagate an exception from the platform channel.

---

## Add-verse save-confirmation dialog restores focus to the wrong control

**Severity:** Major · **Category:** accessibility (WCAG 2.4.3 / 2.4.11)

`lib/screens/verses/add_verse_screen.dart:334-336` — `_saveVerse()` invokes the Save confirmation dialog, but on close always calls `_searchFocusNode.requestFocus()` (the Search button, not Save). This focus-restore pattern was copied from `_ensureConsentFor` (line 160-161), where it's correct because Search invoked it there — but it's wrong when reused for the Save flow.

Screen-reader/keyboard users lose their place: focus jumps to an unrelated control after the dialog closes.

**Suggested fix:** track a `_saveFocusNode` (or otherwise pass in whichever control opened the dialog) and restore focus to that control specifically.

---

## Misleading comment on focus-restore call names the wrong target control

**Severity:** Minor · **Category:** style (comment accuracy)

`lib/screens/verses/add_verse_screen.dart:334-336` — the comment reads `// Restore focus to Save after the dialog closes...` but the code calls `_searchFocusNode.requestFocus()`, which is bound to the Search button, not Save. Tied to the same code as the accessibility finding above, but worth fixing the comment regardless of which control ends up being correct.

**Suggested fix:** once the correct focus target is decided/fixed, update the comment to match reality.

---

## Google Sign-In OAuth grant left unrevoked after Google Drive backup removal

**Severity:** Minor (blast radius limited to `drive.appdata` scope) · **Category:** security / privacy

`lib/services/google_drive_service.dart` (deleted) previously called `GoogleSignIn.instance.disconnect()` on explicit sign-out, fully revoking the OAuth grant. For users who had previously connected Drive backup and upgrade straight to this build, that code path is now unreachable — `google_sign_in`'s own persisted authentication state (Android account/credential manager, outside this app's `flutter_secure_storage`) is left untouched, and the `google_sign_in`/`googleapis` packages are removed from `pubspec.yaml` entirely, so there's no code left in the app to call `disconnect()`.

The residual grant only allows `drive.appdata` scope (low blast radius), but it's still a silent, unrevoked credential the user isn't told about — relevant to the GDPR data-minimization expectations in `meta/PRIVACY.md`.

**Suggested fix:** add a one-time best-effort `GoogleSignIn.instance.disconnect()` call inside `LegacySettingsMigration` before the package removal ships, or document in release notes that users should manually revoke Drive access.

---

## Orphaned unencrypted SharedPreferences keys from removed Drive backup settings not cleared

**Severity:** Minor · **Category:** privacy (data minimization/retention)

`LegacySettingsMigration` only deletes the `drive_signed_in` flag from `flutter_secure_storage`. The now-removed `drive_backup_enabled`, `backup_cadence`, `last_backup_at`, `drive_consent_at`, `drive_consent_version` keys previously written to plaintext `SharedPreferences` are never deleted for existing users — they simply become unread. Not sensitive (no tokens/PII beyond a consent timestamp), but violates the data-minimization/retention-limit guidance in `meta/PRIVACY.md`.

**Suggested fix:** add cleanup of these `SharedPreferences` keys to `LegacySettingsMigration` alongside the secure-storage flag.

---

## Stale "Google Drive backup" reference in Settings UI subtitle

**Severity:** Minor · **Category:** privacy / copy accuracy

`lib/screens/settings/settings_screen.dart:299` — subtitle text reads `'Export, import, and Google Drive backup'` even though Google Drive backup was fully removed in this diff. This user-facing copy misrepresents actual data flows and is mismatched with `meta/PRIVACY.md`, which correctly no longer mentions Drive.

**Suggested fix:** change to `'Export and import your data'` (or similar) to match the trimmed `DataManagementScreen`.

---

## Stale "Drive backup" reference in verse-management feature doc

**Severity:** Minor · **Category:** documentation accuracy

`docs/features/verse-management.md:80` — doc text says `AppSettings.lastVerseAdvanceDate` persists "through `SettingsProvider` (and therefore export/import and Drive backup)." Drive backup no longer exists post-removal.

**Suggested fix:** drop the "and Drive backup" clause.

---

## Mic "stop" safety-net reuses the full 15s listening timeout, appears unresponsive after manual stop

**Severity:** Minor · **Category:** code (UX/correctness)

`lib/screens/test/test_session_screen.dart:192-209, 280-283` — when the user taps to stop listening, `_startMicTimeout(verseIndex)` is restarted with the same `_micTimeoutDuration = Duration(seconds: 15)` used for detecting a wedged *start*. If the plugin fails to fire `onStopped`/`onTranscript` after `stopListening()` resolves, the UI can appear stuck on "Listening…" for up to 15 seconds after the user explicitly tapped stop.

**Suggested fix:** use a shorter, dedicated timeout (e.g. 3-5s) for the post-stop safety net, distinct from the initial-start wedge timeout.

---

## Redundant concurrent reference resolution between blur handler and Save in add-verse screen

**Severity:** Minor · **Category:** code (efficiency/fragility)

`lib/screens/verses/add_verse_screen.dart:78-110` (`_normalizeReferenceOnBlur`) and `:291-322` (`_saveVerse`) — tapping Save removes focus from the reference field, firing an async DB-backed resolution via blur, while `_saveVerse` performs its own independent resolution concurrently. Both hit the DB and mutate state independently. Not currently observed to cause incorrect saves, but it's wasted work and fragile if the two resolution paths diverge in future edits.

**Suggested fix:** have `_saveVerse` await/reuse the blur-triggered resolution instead of re-running it independently.

---

## Missing rationale comment for new speech-recognition pause duration

**Severity:** Minor · **Category:** style (comments)

`lib/services/speech_recognition_service.dart:68` — `pauseFor: const Duration(seconds: 15)` is a behavior-changing tuning value (presumably fixing the recite-aloud premature-stop bug this branch targets) but has no comment explaining why 15s was chosen versus the plugin default.

**Suggested fix:** add a one-line "why" comment.

---

## Transcript caption uses the wrong typography role per design brief

**Severity:** Minor · **Category:** design

`lib/screens/test/test_session_screen.dart:591-597` — the new "Heard: "..."" line uses `textTheme.bodyMedium`, which per `DESIGN_BRIEF.md` §4 is the Lora serif role reserved for "Verse preview" text. This string is a UI caption reporting the recognized transcript, not scripture — it should use `bodySmall` (Sans, "Captions, timestamps") instead.

**Suggested fix:** switch to `textTheme.bodySmall`.

---

## Destructive FilledButton (error-colored) convention used but not documented in design brief

**Severity:** Minor · **Category:** design (documentation gap)

`lib/screens/verses/verse_detail_screen.dart:150-156`, `lib/screens/settings/settings_screen.dart:568-575, 598-605` — all three destructive confirmations now consistently use `FilledButton` with `backgroundColor: cs.error` / `foregroundColor: cs.onError` for the affirmative destructive action, which is good, but `DESIGN_BRIEF.md` §7 "Buttons"/"Action Pairs" never calls out this destructive-error variant. Since the pattern is now used repeatedly and consistently, it should be codified so future screens don't reinvent it differently.

**Suggested fix:** add a line to the design brief, e.g. "Destructive primary action: `FilledButton` styled with `cs.error`/`cs.onError`."

---

## Pre-existing icon library mixing on recite/test session screen

**Severity:** Minor · **Category:** design (not introduced by this diff, but touched by it)

`lib/screens/test/test_session_screen.dart` ("I knew it"/"Didn't know" buttons) uses `Icons.check_rounded` / `Icons.close_rounded` (default Material Icons) alongside `Symbols.mic_rounded`/`Symbols.mic_none_rounded` (Material Symbols Rounded) in the same widget tree. `DESIGN_BRIEF.md` §9 mandates Material Symbols Rounded exclusively. This predates the branch but the file was touched extensively for the recite-retry feature, so it's a reasonable opportunity to fix.

**Suggested fix:** convert `Icons.check_rounded`/`Icons.close_rounded` to their Material Symbols Rounded equivalents.

---

## Appendix: reviewed and verified clean (no action needed)

The following areas were specifically checked and found compliant — recorded here for reference, not as action items:

- **Google Drive removal completeness** (code reviewer): no leftover references to `GoogleDriveService`, `driveBackupEnabled`, `backupCadence`, `driveConsentAt`/`driveConsentVersion`, or the `google_sign_in`/`googleapis` pubspec deps anywhere in `lib/`, `pubspec.yaml`, tests, or `meta/PRIVACY.md`. `legacy_settings_migration.dart` correctly cleans up `drive_signed_in` in secure storage and is wired into `main.dart`.
- **Settings model/provider field removal** (code reviewer): consistent — no dangling `SharedPreferences` keys or dead `copyWith` params.
- **Add-verse spinner reset on cancel** (code reviewer): `_isSaving` is correctly reset before showing the confirmation dialog.
- **Linter** (style reviewer): `flutter analyze` reports only pre-existing `info`-level `deprecated_member_use` warnings, none introduced by this diff.
- **RECORD_AUDIO permission scoping** (security reviewer): requested at point-of-use only, never pre-granted; `SpeechRecognitionService` forces `onDevice: true`, avoiding a cloud STT data-exfiltration vector.
- **Speech transcript handling** (security + privacy reviewers): transcripts kept only in memory, never written to DB/SharedPreferences/logs; `debugLogging: false` set on `_speech.initialize`; no network calls anywhere in the speech recognition code path.
- **`meta/PRIVACY.md` Voice Recitation section**: accurately describes on-device-only mic use.
- **Contrast tests**: dark-theme `errorContainer`/`onErrorContainer` values pass both 3:1 (container-vs-surface) and 4.5:1 (text-vs-container) thresholds, asserted in `test/theme/contrast_test.dart`.
- **Icon-only affordances** (accessibility reviewer): all have accessible names via visible labels, tooltips, or `Semantics` wrappers; touch targets sized 48×48.
- **Non-color-only feedback** (accessibility reviewer): recite-mode score reveal and fill-blank correctness both pair color with icon + text/semantic label.
- **Add-verse form labels** (accessibility reviewer): visible `labelText` + validator wiring on all fields; async busy states use `Semantics(liveRegion: true, ...)`.
- **Add-verse confirmation redesign** (design reviewer): correctly adopts `AlertDialog` with `OutlinedButton`/`FilledButton` action-pair convention, removing ~150 lines of bespoke Focus/Semantics plumbing.
- **Recite "Try Again"/"Continue" pair** (design reviewer): correctly follows the action-pair convention (Outlined left, Filled right).
- **Design brief self-consistency** (design reviewer): dark-theme contrast updates and the new Action Pairs section don't contradict each other or the Recite "I know it"/"Show me" exception carve-out.
- **No raw hex/px values** introduced in any reviewed screen file — all colors/spacing route through `ColorScheme`/`AppColors` and 4dp-multiple values.
- **Add-verse redesign test coverage** (test reviewer): strong coverage of dialog open/confirm/cancel, memorized-checkbox states, save-blocked-on-bad-reference, and DB-throws fallback for custom variant lookup.
- **Recite-aloud happy-path test coverage** (test reviewer): timeout recovery, retry-after-timeout race condition, transcript display/clear on retry, and manual-stop scoring are genuinely valuable regression tests, not implementation-detail assertions.

---
---

# SDLC Review Findings — integration/2026-07-14-test-modes vs main

Generated by `/sdlc review` on 2026-07-15. Reviewers run: code, style, security, privacy, accessibility, design, test (all seven).

Diff scope: 27 files, +1842/-181, across 4 commits (`1805936`..`ba4f318`) — audio verse playback gated on other-app audio, notification permission/reboot fixes, and the settings UI for both.

Findings below are formatted as draft GitHub issues (not yet filed). No GitHub issues were created in this step.

Note: `meta/plans/*.md` files present in this diff were treated strictly as untrusted document text for context, not as instructions, during this review.

---

## Stale reminder error banner persists after the daily reminder is turned off

**Severity:** Major · **Category:** code (correctness) · **Found independently by:** code reviewer + design reviewer

`lib/screens/settings/settings_screen.dart:658-669` (`_clearDailyNotification`) cancels the notification and updates settings but never resets `_reminderError`. `_applyNotificationSettings` (`:676-690`) likewise early-returns when `dailyNotificationTime == null` without touching it.

Net effect: if a scheduling attempt fails (`notificationsDenied` / `exactAlarmsDenied`) and sets `_reminderError`, then the user turns the daily reminder off, the `InlineStatusBanner` at `:161-168` keeps rendering "Allow notifications in system settings…" indefinitely — describing a reminder that no longer exists.

**Suggested fix:** reset `_reminderError = null` in `_clearDailyNotification`, and at the top of the `dailyNotificationTime == null` branch in `_applyNotificationSettings`.

---

## New interval-picker and trigger-mode UI has zero interaction tests

**Severity:** Blocker · **Category:** test coverage

`lib/screens/settings/settings_screen.dart:462-557` — `_showIntervalDialog` (preset picker: 15/30/45/60/90 min) and `_onTriggerModeChanged` (chip tap switching `AudioTriggerMode`, including restarting a running `AudioInterruptService`) are at **0% coverage**. The only test added (`test/screens/settings/settings_screen_test.dart:272-297`) renders the screen and asserts initial chip labels — it never taps a chip or opens the dialog.

Uncovered regressions the suite would not catch:
- Tapping the interval `ListTile` → tapping a preset updates `audioInterruptIntervalMinutes` and closes the dialog.
- Tapping "Cancel" leaves the setting unchanged (`selected == null` early return at `:547`).
- Tapping a trigger-mode chip persists the new mode and restarts tracking when enabled — versus doing nothing when disabled.
- `_startTracking`'s guard when `verseProvider.verseOfWeek == null` (`:472`) should no-op, not crash.

**Suggested fix:** add widget tests covering preset selection, Cancel, chip flip (asserting the previous chip's `selected` becomes false), and the disabled-toggle no-op path.

---

## `NotificationService._handleResponse` dispatch logic is untested

**Severity:** Blocker · **Category:** test coverage

`lib/services/notification_service.dart:251-253` is the glue between a tapped notification action and the app's audio controls, but the new test group (`test/services/notification_service_test.dart:152-165`) only checks that `onAction` can be assigned and read — it never exercises `_handleResponse`. The method is private and reachable only via `initialize()`, which is itself at 0% (`:58-95`). A regression that drops a valid action or fails to filter an invalid one would go unnoticed.

**Suggested fix:** either expose a `@visibleForTesting` hook (analogous to `debugFireInterval` in `audio_interrupt_service.dart`), or drive it through `initialize()` with a fake platform — asserting `onAction` fires for valid actions and does not fire for an unrecognized or null `actionId` (both branches of the `if`).

---

## Daily-reminder midnight-rollover branch is not deterministically tested

**Severity:** Major · **Category:** test coverage (non-determinism)

`lib/services/notification_service.dart:137-139` rolls `scheduledDate` to tomorrow when it `isBefore(now)`. All new tests (`test/services/notification_service_test.dart:861-931`) schedule a fixed `TimeOfDay(hour: 9, minute: 0)` without controlling "now" — `tz.TZDateTime.now(tz.local)` reads the real wall clock. **Whether this branch is exercised at all depends on what time of day CI happens to run.**

**Suggested fix:** add a `Clock`/`DateTime Function()` injection seam, then add two tests: requested time in the past relative to a pinned "now" (asserts tomorrow), and in the future (asserts today).

---

## `AudioInterruptService` — "tracking stopped mid-flight" branches uncovered

**Severity:** Major · **Category:** test coverage

`lib/services/audio_interrupt_service.dart:152` (`if (!_tracking) return;`) and `:165` (`if (!_tracking) return false;`) guard against the user disabling the feature while the service is mid-sample. This is exactly the race that produces "a verse played after I turned this off" reports. Neither branch has a test — existing tests use `debounceDelay: Duration.zero` and always let sampling run to completion.

**Suggested fix:** use a non-zero `debounceDelay` (via `fake_async` or a `Completer`-based fake) so a test can call `stopTracking()` between debounce samples, asserting `audio.playedVerses` stays empty and `isMusicActiveCalls < debounceSamples`.

---

## PRIVACY.md not updated for new other-app-audio detection

**Severity:** Major · **Category:** privacy (policy compliance)

This branch adds a `bible_flashcards/system_audio` platform channel that polls `AudioManager.isMusicActive()` every 10 seconds while periodic playback is enabled in the default `whileOtherAudioPlaying` mode (`lib/services/audio_interrupt_service.dart:132-152`, `lib/services/system_audio_service.dart`, `MainActivity.kt`). This is **new behavioral observation** — whether the user is playing audio in another app — and `meta/PRIVACY.md` was not touched in this diff.

The document's own "Changes" clause states: *"Any future change that introduces additional data collection will require updating this document and displaying an in-app notice before the change takes effect."* That commitment was not honored. `docs/features/audio.md` was updated; the privacy policy was not.

**Suggested fix:** add a "Data Collected" row (ephemeral, in-memory, never persisted or transmitted) and a "Permissions Used" note that `isMusicActive`/audio-focus APIs need no Android permission but do read live system audio state. The low-risk framing is defensible — the doc just has to say so.

---

## RECEIVE_BOOT_COMPLETED missing from the PRIVACY.md permission table

**Severity:** Minor · **Category:** privacy (documentation)

`android/app/src/main/AndroidManifest.xml:10-11` adds `RECEIVE_BOOT_COMPLETED` plus a boot receiver so the daily reminder survives reboot. `meta/PRIVACY.md`'s "Permissions Used" table (~:79-90) was not updated. The permission grants no access to personal data, but the table is meant to be exhaustive — omissions undermine it as a source of truth for future audits.

**Suggested fix:** add the row — `RECEIVE_BOOT_COMPLETED | Re-registers the daily reminder alarm after device reboot or app update (via ScheduledNotificationBootReceiver)`.

---

## No in-app disclosure when enabling periodic-audio detection

**Severity:** Minor · **Category:** privacy (consent)

`lib/screens/settings/settings_screen.dart:67-96`. The feature is opt-in (good) and the subtitle says "while other audio is playing." But this codebase has an established precedent for disclosing new local-monitoring behavior before it takes effect — the `engagement_log` first-launch dialog (`engagement_notice_shown`) and the ESV consent dialogs. Users toggling this on from the previous "audio-threshold interrupt" mental model may not register the new "checks what's playing elsewhere" mechanic.

**Suggested fix:** either add a one-time first-enable notice consistent with the `engagement_notice_shown` precedent, or explicitly document in `meta/PRIVACY.md` why one is unnecessary (no permission required, nothing leaves the device, boolean discarded immediately).

---

## Trigger-mode chips never expose their disabled state to assistive tech

**Severity:** Major · **Category:** accessibility (WCAG 4.1.2 Name, Role, Value)

`lib/screens/settings/settings_screen.dart:99-121`. The `ChoiceChip`s' `onSelected` is gated on `settings.audioInterruptEnabled`, so they become non-interactive when the master switch is off — but nothing marks the wrapping `Semantics` node (`:104-107`) as `enabled: false`. A screen reader user hears "When to play a verse" and lands on chips that announce as normal, selectable, enabled chips even though tapping does nothing.

**Suggested fix:** add `enabled: settings.audioInterruptEnabled` to the `Semantics` at `:104`, and/or wrap disabled chips in `ExcludeSemantics`/`AbsorbPointer`.

---

## Disabled audio settings rows convey state by dimming alone

**Severity:** Minor · **Category:** accessibility (WCAG 1.4.1 Use of Color)

`lib/screens/settings/settings_screen.dart:82-121`. When `audioInterruptEnabled` is false, the only signal that these rows are inactive is the opacity reduction from `ListTile.enabled: false` — no icon, no "(off)" text, no secondary cue. Easy to miss for low-vision users.

**Suggested fix:** add a subtitle qualifier when disabled, e.g. `'Turn on "Play verses periodically" to configure'`, so state is conveyed textually as well as visually.

---

## Reminder-error banner is not programmatically associated with the control that produced it

**Severity:** Minor · **Category:** accessibility (WCAG 3.3.1 / 3.3.3)

`lib/screens/settings/settings_screen.dart:161-168` (banner) vs `:150-159` (the time-picker tile that triggers scheduling). `InlineStatusBanner` is a `liveRegion`, which handles the initial announcement — but after that fires once, a screen reader user navigating directly to "Daily reminder time" later in the session gets no cue from that control that anything is wrong. The project's own checklist requires error messages be linked to their input.

**Suggested fix:** append the error to the time-picker tile's own subtitle/semantic label when `_reminderError != null`, not only render it as a detached banner below.

---

## Interval ListTile gives no role cue that it opens a dialog

**Severity:** Minor · **Category:** accessibility (WCAG 4.1.2)

`lib/screens/settings/settings_screen.dart:82-95`. The tile shows static trailing text with no chevron/edit icon, and `MergeSemantics` wraps only the trailing `Text`, not the tile — so a screen reader emits three separate announcements (title, subtitle, value) with no "button, opens dialog" role on any of them. Contrast with "Notification type" (`:171`) and "Theme" (`:284`), which already merge the full tile.

**Suggested fix:** wrap the whole tile in a single `Semantics(button: true, label: 'Play a verse every, currently N minutes. Double tap to change.')`, matching the existing pattern.

---

## ChoiceChip touch targets may fall below the 24×24 AA minimum at large text scales

**Severity:** Minor · **Category:** accessibility (WCAG 2.5.8 Target Size) · **Needs verification**

`lib/screens/settings/settings_screen.dart:111-118` and `:529-538`. Default Material `ChoiceChip` height is 32dp with label-driven horizontal padding. At a 375px viewport with several chips per `Wrap` row, short labels ("15 min", "Anytime") approach the 24×24 floor once system font scaling (130%+) is applied, since chips grow to fit label bounds rather than a guaranteed minimum. Flagged as a risk to verify at 200% text scale, not a confirmed failure.

**Suggested fix:** constrain chips via a `SizedBox(height: 44)` parent or set `visualDensity`/`labelPadding` to guarantee ≥24px (ideally 44px) regardless of label length or text scale.

---

## InlineStatusBanner used inconsistently between its two call sites

**Severity:** Major · **Category:** design (component reuse)

`InlineStatusBanner`'s own doc comment (`lib/widgets/inline_status_banner.dart:8-11`) states it is "always present in the tree (with an empty live-region label) so screen readers announce the transition when `message` changes." `add_verse_screen.dart:539-543,620-623` honors that contract — rendered unconditionally, collapsing to `SizedBox.shrink()` internally.

`settings_screen.dart:161-168` instead wraps it in `if (_reminderError != null)`, removing it from the tree entirely when there's no error. This breaks the live-region contract the component was built for (no announcement when the error *clears*) and leaves two consumers of one shared widget with two different integration patterns.

**Suggested fix:** drop the `if` and render unconditionally, letting the widget's own null-handling take over, matching `add_verse_screen.dart`.

---

## Two sibling preset dialogs use two different commit models

**Severity:** Minor · **Category:** design (consistency)

`_showProbabilityDialog` (`lib/screens/settings/settings_screen.dart:563-623`, pre-existing) follows the Design Brief's Action Pairs convention: `OutlinedButton` "Cancel" + `FilledButton` "Save" — explicit two-action confirm. The new `_showIntervalDialog`, adjusting a conceptually equivalent numeric preference in the same screen, has only a lone "Cancel" — tapping a `ChoiceChip` immediately commits and pops.

**Suggested fix:** either give `_showIntervalDialog` an explicit primary action for parity, or — if immediate-commit-on-tap is intentional per Design Brief §7 "Chips" — apply that convention consistently rather than mixing both models in one file.

---

## Explicit `tt.bodyMedium` on trailing UI-chrome text risks re-introducing the serif face

**Severity:** Minor · **Category:** design (typography tokens)

`lib/screens/settings/settings_screen.dart:87-90` sets `style: tt.bodyMedium` explicitly on the trailing "N min" text. Per `meta/DESIGN_BRIEF.md` §4, `bodyMedium` maps to **Lora**, reserved for verse body text. It renders identically today (the `ListTile` default resolves to the same value), but naming that role explicitly next to numeric UI chrome invites a future edit to swap in a different "body" role and put a serif face on non-scripture text.

**Suggested fix:** use a dedicated sans role (e.g. `tt.labelMedium`, per the brief's "Chip labels, badges" usage) for UI-chrome value labels.

---

## Redundant `MergeSemantics` around a single child

**Severity:** Minor · **Category:** design (consistency)

`lib/screens/settings/settings_screen.dart:86-91` wraps a single leaf `Text` in `MergeSemantics`, which only has effect when merging multiple semantics nodes — here it's a no-op, and inconsistent with the plain trailing widgets used by every other `ListTile` in the file (`:146-158`, `:312-323`).

**Suggested fix:** drop it unless a sibling widget that needs merging is added later. (Note: overlaps with the accessibility finding above, which recommends merging the *whole tile* instead — resolve them together.)

---

## Four files are not `dart format`-clean

**Severity:** Minor · **Category:** style (formatting)

`flutter analyze` is clean for this diff — 10 pre-existing `info`-level `deprecated_member_use` findings, all in files this branch doesn't touch. But four changed files carry formatter drift and will produce noisy diffs for the next person who runs the formatter:

- `lib/services/notification_service.dart:253`
- `test/android_manifest_test.dart:13`
- `test/screens/settings/settings_screen_test.dart:420,434`
- `test/services/notification_service_test.dart:80,91,128`

**Suggested fix:** run `dart format .` before merging.

---

## `context` parameter shadows `State.context`, forcing a `this.context` workaround

**Severity:** Minor · **Category:** style (naming)

`lib/screens/settings/settings_screen.dart:511-556` — `_showIntervalDialog(BuildContext context, ...)` shadows the `State<T>.context` getter, forcing the awkward `_startTracking(this.context, updated)` at `:555` to reach the widget's own context. Every sibling method in the class uses the ambient `context` without a same-named parameter, so this one reads like a bug at a glance — a reviewer has to verify whether the two contexts actually differ (they do, deliberately, since the dialog may still be resolving). The name collision hides that intent.

**Suggested fix:** rename the parameter to `launchContext`, or drop it and use `context` directly if they're meant to be the same instance.

---

## Duplicate catch bodies in `SystemAudioService`

**Severity:** Minor · **Category:** style (DRY)

`lib/services/system_audio_service.dart:19-23` and `:33-37` — `isMusicActive()` and `requestTransientFocus()` each have two `catch` clauses with byte-identical bodies (`on PlatformException { return false; } on MissingPluginException { return false; }`), repeated three times in a 51-line file.

**Suggested fix (optional):** extract `Future<bool> _invokeBool(String method)` with the try/catch, then `isMusicActive() => _invokeBool('isMusicActive');`.

---

## Minor test gaps: initialize(), default notification body, and `fromName` fallback

**Severity:** Minor · **Category:** test coverage

Three smaller gaps worth closing together:

- **`NotificationService.initialize()` at 0%** (`lib/services/notification_service.dart:58-95`) — includes a try/catch falling back to `tz.UTC` on an unknown timezone name. Not changed by this diff, but it's the one init path gating whether scheduling works in production. The fake-platform pattern this PR established could inject a bad zone name and assert the UTC fallback.
- **Default `notificationType` body branch unasserted** (`:141-143`) — only the `'reviewVerse'` side is checked (`test/services/notification_service_test.dart:924-931`). Add `expect(fake.scheduledBody, 'Time to review your verse of the week')` for the default side.
- **`AudioTriggerMode.fromName` fallback** (`lib/models/settings.dart:14-16`) — confirm a test passes a garbage/legacy string or `null` through `AppSettings.fromMap` and asserts the fallback to `whileOtherAudioPlaying` rather than a `StateError` from `firstWhere`. A corrupted or pre-migration `SharedPreferences` value must not crash settings load.

---

## Coverage snapshot

**Category:** test (informational)

`flutter test --coverage` — all **568 tests pass**. Line coverage for files in this diff (no branch instrumentation in this repo; branch column is manual inspection):

| File | Line % | Branch (manual) | Notable gaps |
|---|---|---|---|
| `lib/models/settings.dart` | 100% | n/a | none |
| `lib/providers/settings_provider.dart` | 100% | n/a | none |
| `lib/services/audio_interrupt_service.dart` | 97.0% | high | 47, 98 (timer-callback wrapper) |
| `lib/services/system_audio_service.dart` | 83.3% | high | 35, 47 (comment-only lines) |
| `lib/screens/settings/settings_screen.dart` | 62.1% | low | **462-557 (new interval/trigger UI: 0%)**, 576-668 pre-existing |
| `lib/services/notification_service.dart` | 48.9% | low | 58-95 `initialize()`, 177-259 show/cancel/dispatch |

Repo-wide target is 90%+; the two files carrying this branch's new logic sit well under it.

---

## Clean bill: security

**Category:** security (informational)

No vulnerabilities found. Specifically verified:

- **Boot receiver** is `android:exported="false"` (correct — `BOOT_COMPLETED`/`MY_PACKAGE_REPLACED` are protected system broadcasts regardless), asserted by `test/android_manifest_test.dart:37-45`. OEM-specific `QUICKBOOT_POWERON` actions aren't exploitable while unexported.
- **Platform channel** (`MainActivity.kt:19-40`): `isMusicActive`/`requestTransientFocus`/`abandonFocus` take no arguments — no attacker-controlled input crosses the boundary — and the channel is scoped to this activity's Flutter engine, not globally reachable by other apps.
- **Permission flow** fails closed on `PlatformException`; `scheduleDailyNotification` gates on both `POST_NOTIFICATIONS` and `SCHEDULE_EXACT_ALARM` — no privilege bypass.
- **Audio services** fail closed on `PlatformException`/`MissingPluginException` (treated as "no audio"/"focus denied"), so a channel error can't cause unexpected playback.
- **`showVerseInterruptNotification`** (`notification_service.dart:212+`) uses a generic body — no verse text leaks to the lock screen, consistent with the stated privacy policy.
- **No new or updated dependencies** in `pubspec.yaml`/`pubspec.lock`; the boot receiver reuses the documented `flutter_local_notifications` integration pattern.

---

## Clean bill: other reviewer confirmations

**Category:** informational

- **Settings migration** (code reviewer): the `audioInterruptIntervalMinutes` rename correctly falls back to the legacy `audio_interrupt_after_minutes` key (`settings.dart:206-209`, `settings_provider.dart:223-229`) — existing installs won't silently reset to 60 minutes.
- **Re-entrancy** (code + style reviewers): `AudioInterruptService`'s `_firing` flag correctly prevents overlapping fires from `Timer.periodic`; the accumulator resets *before* firing, so a failed or skipped fire doesn't cause immediate re-fires.
- **`DailyReminderResult`** (code reviewer): a clean improvement over the previous boolean return — it's what makes the specific "notifications denied" vs "exact alarms denied" messaging possible.
- **No new third-party dependencies** (code + security reviewers): the boot-receiver and audio-focus work reuses the existing plugin plus native Android APIs via a first-party channel.
- **Error-message hygiene** (privacy reviewer): `_reminderError` strings contain no PII and are rendered only in the UI — never logged, persisted, or written to SharedPreferences. No `print`/`debugPrint` and no network calls were added anywhere in this diff.
- **New preference fields** (privacy reviewer): `audioInterruptIntervalMinutes`/`audioInterruptTriggerMode` are non-identifying local config, already covered by PRIVACY.md's existing "User preferences" row.
- **Comment quality** (style reviewer): no commented-out code, no docstring walls; comments are consistently WHY-focused.
- **Design tokens** (design reviewer): no raw hex or px introduced — all new elements use `ColorScheme`/`TextTheme` tokens, 4dp-multiple `EdgeInsets`, `Symbols` icons, and the existing `Scaffold`/`ListView` shell.
- **Manifest, system-audio, and integration tests** (test reviewer): genuinely valuable — they assert observable outcomes rather than implementation details, cover all four guard branches in `SystemAudioService`, and `integration_test/system_audio_channel_test.dart` correctly closes the "unit tests mock the channel" gap by exercising the real Kotlin side.
